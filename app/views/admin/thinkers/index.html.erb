<header>
	<h1>Thinkers </h1>
</header>

	<table class="table">
  <thead>
    <tr>
      <th>Username</th>
      <th>Email</th>
      <th>Created</th>
      <th>Thoughts</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody class="js-thinkers">
  	 <% @thinkers.each do |thinker| %>
    	<tr>
    		<td><%= link_to thinker.username, admin_thinker_path(thinker) %></td>
    		<td><%= thinker.email %></td>
    		<td><%= time_format(thinker.created_at) %></td>
    		<td><%= link_to thinker.thoughts.count, thinker_path(thinker) %></td>
    		
    		</td>
     
   		 </tr>
     <% end %>
   
  </tbody>
</table>
