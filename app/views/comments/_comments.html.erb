<%# HAS CODE FROM INDEX.HTML.ERB %>

<ol>
  <% @thought.comments.each do |comment| %>
  <li><blockquote>
	<%= simple_format(comment.body) %>
	<footer>
		<%= time_ago_in_words(comment.created_at) %> ago by : <%= comment.thinker %>
	</footer>
</blockquote></li>
  <% end %>
</ol>