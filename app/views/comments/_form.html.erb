<h3>Add Comment</h3>

<%= form_for [@thought, @comment] do |f| %>
  

  <div class="field"> 
    <%= f.text_area :body, class: "form-control", required: true %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit "Submit Comment", class: "btn btn-success", id: "subBtn" %>
  </div>
<% end %>

